# 🤖 Настройка Google Gemini API

## Обзор

Платформа "Ал Хоразми" теперь использует **Google Gemini API** для:
- 🎯 **Генерации задач** - ИИ создает уникальные математические задачи
- ✅ **Проверки решений** - ИИ анализирует ответы пользователей
- 📸 **Анализа фото** - ИИ проверяет рукописные решения (в разработке)

---

## 📋 Шаг 1: Получение API Ключа

### 1.1 Перейдите на Google AI Studio

Откройте: https://aistudio.google.com/app/apikey

### 1.2 Создайте API ключ

1. Нажмите **"Get API key"** или **"Create API key"**
2. Выберите проект Google Cloud (или создайте новый)
3. Скопируйте сгенерированный ключ

**Пример ключа:**
```
AIzaSyD1234567890abcdefghijklmnopqrstuvwxyz
```

⚠️ **ВАЖНО:** Храните ключ в секрете! Не публикуйте его в Git!

---

## 🔧 Шаг 2: Настройка Проекта

### 2.1 Установите зависимости

```bash
# Активируйте виртуальное окружение
venv\Scripts\activate

# Установите обновленные зависимости
pip install -r requirements.txt
```

Это установит `google-genai==1.0.0`

### 2.2 Добавьте API ключ в .env

Откройте файл `.env` и добавьте:

```env
# Google Gemini API
GEMINI_API_KEY=ваш-реальный-ключ-здесь
```

**Пример:**
```env
GEMINI_API_KEY=AIzaSyD1234567890abcdefghijklmnopqrstuvwxyz
```

### 2.3 Проверьте настройки

Убедитесь, что файл `.env` содержит:

```env
# Django Settings
SECRET_KEY=django-insecure-dev-key-please-change-in-production-12345
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1

# Google Gemini API
GEMINI_API_KEY=ваш-ключ-здесь

# CORS Settings
CORS_ALLOWED_ORIGINS=http://localhost:8000,http://127.0.0.1:8000

# JWT Settings
JWT_ACCESS_TOKEN_LIFETIME=60
JWT_REFRESH_TOKEN_LIFETIME=1440
```

---

## 🚀 Шаг 3: Запуск с Gemini AI

### 3.1 Запустите сервер

```bash
python manage.py runserver
```

### 3.2 Откройте приложение

Перейдите на: http://localhost:8000

### 3.3 Проверьте работу

1. **Зарегистрируйтесь** или войдите
2. На дашборде нажмите **"🔄 Новая задача"**
3. Вы увидите метку **"🤖 Сгенерировано ИИ"**
4. Решите задачу и отправьте ответ
5. ИИ проверит ваше решение!

---

## 🎯 API Endpoints

### Генерация задачи через ИИ

```http
GET /api/problems/generate-ai/
Authorization: Bearer {token}
```

**Параметры (опционально):**
- `topic` - название темы

**Ответ:**
```json
{
  "problem": {
    "id": "problem_1_Квадратное_уравнение",
    "topic_name": "Алгебра: Квадратные уравнения",
    "title": "Квадратное уравнение с параметром",
    "latex_formula": "x^2 - 5x + 6 = 0",
    "description": "Решите уравнение и найдите сумму корней",
    "difficulty_score": 1200,
    "hints": ["Используйте теорему Виета", "Факторизация"],
    "generated_by_ai": true
  },
  "user_index": 1520,
  "difficulty_range": {
    "min": 1420,
    "max": 1570
  }
}
```

### Проверка решения через ИИ

```http
POST /api/problems/submit-ai/
Authorization: Bearer {token}
Content-Type: multipart/form-data

{
  "problem_id": "problem_1_...",
  "submitted_answer": "11",
  "solution_photo": [файл]
}
```

**Ответ:**
```json
{
  "is_correct": true,
  "points_awarded": 150,
  "index_change": 15,
  "new_index": 1535,
  "correct_answer": "11",
  "solution_steps": [
    "Шаг 1: Используем теорему Виета...",
    "Шаг 2: Сумма корней = -b/a = 5"
  ],
  "ai_feedback": "Отличная работа! Ответ правильный.",
  "ai_explanation": "Вы верно применили теорему Виета",
  "confidence": 0.95
}
```

---

## ⚙️ Настройки Frontend

В файле `static/app.js` есть переключатель:

```javascript
// Настройка: использовать ли Gemini AI (по умолчанию true)
const USE_GEMINI_AI = true;
```

**Опции:**
- `true` - использовать Gemini AI (рекомендуется)
- `false` - использовать статические задачи из БД

---

## 🧪 Тестирование

### Тест 1: Проверка подключения

Создайте файл `test_gemini.py`:

```python
from core.gemini_service import get_gemini_service

# Инициализация
gemini = get_gemini_service()

# Генерация задачи
problem = gemini.generate_problem(
    topic="Алгебра: Квадратные уравнения",
    difficulty=1000,
    user_level=1000
)

print("Задача сгенерирована:")
print(f"Название: {problem['title']}")
print(f"Формула: {problem['latex_formula']}")
print(f"Ответ: {problem['correct_answer']}")
```

Запустите:
```bash
python test_gemini.py
```

### Тест 2: Проверка решения

```python
from core.gemini_service import get_gemini_service

gemini = get_gemini_service()

problem_data = {
    'title': 'Квадратное уравнение',
    'latex_formula': 'x^2 - 5x + 6 = 0',
    'description': 'Найдите сумму корней',
    'correct_answer': '5'
}

result = gemini.check_solution(
    problem_data=problem_data,
    user_answer='5'
)

print(f"Правильно: {result['is_correct']}")
print(f"Обратная связь: {result['feedback']}")
```

---

## 🔍 Отладка

### Проблема: "GEMINI_API_KEY не установлен"

**Решение:**
1. Проверьте файл `.env`
2. Убедитесь, что ключ правильно скопирован
3. Перезапустите сервер

### Проблема: "Ошибка парсинга JSON от Gemini"

**Причина:** Gemini иногда возвращает текст с markdown форматированием

**Решение:** Код автоматически очищает markdown. Если проблема сохраняется:
1. Проверьте промпты в `core/gemini_service.py`
2. Убедитесь, что используется модель `gemini-2.0-flash-exp`

### Проблема: "Задачи генерируются медленно"

**Причина:** Gemini API может иметь задержки

**Решение:**
1. Используйте кеширование (уже реализовано)
2. Рассмотрите использование асинхронных запросов
3. Предзагружайте задачи в фоне

---

## 💡 Промпты для Gemini

### Генерация задачи

Промпт находится в `core/gemini_service.py`:

```python
prompt = f"""Ты - эксперт по математике и педагог. Создай математическую задачу.

ТЕМА: {topic}
СЛОЖНОСТЬ: {difficulty} (шкала 0-3000)
УРОВЕНЬ ПОЛЬЗОВАТЕЛЯ: {user_level}

ТРЕБОВАНИЯ:
1. Задача должна соответствовать теме и уровню
2. Формула в формате LaTeX (без $$ или $)
3. Конкретный ответ
4. Минимум 4 шага решения
5. 2-3 подсказки

ФОРМАТ ОТВЕТА (строго JSON):
{{
    "title": "...",
    "latex_formula": "...",
    "description": "...",
    "correct_answer": "...",
    "solution_steps": [...],
    "hints": [...],
    "difficulty_score": {difficulty}
}}
"""
```

**Можете модифицировать** для улучшения качества задач!

---

## 📊 Мониторинг

### Логи Gemini запросов

Добавьте в `settings.py`:

```python
LOGGING = {
    'version': 1,
    'handlers': {
        'file': {
            'class': 'logging.FileHandler',
            'filename': 'gemini.log',
        },
    },
    'loggers': {
        'core.gemini_service': {
            'handlers': ['file'],
            'level': 'INFO',
        },
    },
}
```

### Статистика использования

Gemini API имеет лимиты:
- **Бесплатный tier:** 60 запросов в минуту
- **Платный tier:** больше запросов

Отслеживайте использование на: https://aistudio.google.com/

---

## 🎓 Примеры Тем для Генерации

```python
topics = [
    "Алгебра: Квадратные уравнения",
    "Алгебра: Иррациональные уравнения",
    "Геометрия: Треугольники",
    "Геометрия: Окружности",
    "Теория чисел: Делимость",
    "Теория чисел: Модульная арифметика",
    "Комбинаторика: Перестановки",
    "Комбинаторика: Сочетания",
    "Математический анализ: Производные",
    "Математический анализ: Интегралы",
    "Теория вероятностей: Классическая вероятность",
    "Линейная алгебра: Матрицы"
]
```

---

## 🚀 Production Рекомендации

### 1. Безопасность

- ✅ Храните `GEMINI_API_KEY` в секретах (не в Git)
- ✅ Используйте переменные окружения
- ✅ Ограничьте доступ к API ключу

### 2. Производительность

- ✅ Кешируйте сгенерированные задачи
- ✅ Используйте асинхронные запросы
- ✅ Настройте rate limiting

### 3. Мониторинг

- ✅ Логируйте все запросы к Gemini
- ✅ Отслеживайте ошибки
- ✅ Мониторьте использование квоты

### 4. Резервный план

- ✅ Храните статические задачи в БД
- ✅ Переключайтесь на БД при ошибках API
- ✅ Уведомляйте администратора об ошибках

---

## 📞 Поддержка

### Документация Gemini

- **Официальная документация:** https://ai.google.dev/docs
- **Python SDK:** https://github.com/google/generative-ai-python
- **Примеры:** https://github.com/google-gemini/cookbook

### Проблемы с API

Если возникли проблемы:
1. Проверьте статус API: https://status.cloud.google.com/
2. Проверьте квоты: https://aistudio.google.com/
3. Изучите логи в `gemini.log`

---

## ✅ Чеклист Настройки

- [ ] Получен API ключ от Google AI Studio
- [ ] Ключ добавлен в `.env` файл
- [ ] Установлен пакет `google-genai`
- [ ] Сервер запущен без ошибок
- [ ] Задачи генерируются с меткой "🤖 Сгенерировано ИИ"
- [ ] ИИ проверяет решения
- [ ] Индекс обновляется после решения

---

**Готово! Теперь ваша платформа использует мощь Gemini AI! 🚀🤖**
