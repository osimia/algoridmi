# 📂 Структура Шаблонов

Шаблоны разделены на модульную структуру для удобства поддержки и разработки.

## 📁 Структура директорий

```
templates/
├── base.html                    # Базовый шаблон (HEAD, scripts)
├── index.html                   # Главная страница (собирает все компоненты)
├── index_old.html              # Старая версия (backup)
├── components/                  # Переиспользуемые компоненты
│   ├── auth_modal.html         # Модальное окно входа/регистрации
│   ├── navbar.html             # Навигационная панель
│   └── styles.html             # CSS стили
└── pages/                       # Отдельные страницы/секции
    ├── dashboard.html          # Дашборд (лаборатория задач)
    ├── arena.html              # Арена соревнований
    └── profile.html            # Профиль пользователя
```

## 🎯 Преимущества новой структуры

1. **Модульность** - каждый компонент в отдельном файле
2. **Переиспользование** - компоненты можно использовать в разных местах
3. **Легкость поддержки** - изменения в одном месте отражаются везде
4. **Читаемость** - код легче читать и понимать
5. **Масштабируемость** - легко добавлять новые страницы/компоненты

## 🔧 Использование

### Базовый шаблон (`base.html`)
Содержит:
- Meta теги
- Подключение TailwindCSS
- Подключение KaTeX (для математических формул)
- Стили через `{% include 'components/styles.html' %}`
- Блоки `{% block content %}` и `{% block extra_scripts %}`

### Главная страница (`index.html`)
Расширяет `base.html` и включает:
```django
{% extends "base.html" %}
{% include 'components/auth_modal.html' %}
{% include 'components/navbar.html' %}
{% include 'pages/dashboard.html' %}
{% include 'pages/arena.html' %}
{% include 'pages/profile.html' %}
```

## ✏️ Как добавить новый компонент

1. Создайте файл в `components/` или `pages/`
2. Добавьте `{% include 'path/to/component.html' %}` в нужное место
3. Перезагрузите страницу

## 📝 Примеры изменений

### Изменить стили
Редактируйте: `templates/components/styles.html`

### Изменить навигацию
Редактируйте: `templates/components/navbar.html`

### Добавить новую страницу
1. Создайте `templates/pages/new_page.html`
2. Добавьте `{% include 'pages/new_page.html' %}` в `index.html`
3. Добавьте соответствующую логику в JavaScript

## 🔄 Откат к старой версии

Если что-то пошло не так:
```bash
mv templates/index.html templates/index_new.html
mv templates/index_old.html templates/index.html
```

## 🚀 Django Template Tags

### Наследование
```django
{% extends "base.html" %}
```

### Включение
```django
{% include 'components/navbar.html' %}
```

### Блоки
```django
{% block content %}
  <!-- Ваш контент -->
{% endblock %}
```

## 📚 Дополнительно

- [Django Templates Documentation](https://docs.djangoproject.com/en/4.2/topics/templates/)
- [TailwindCSS](https://tailwindcss.com/)
- [KaTeX](https://katex.org/)
