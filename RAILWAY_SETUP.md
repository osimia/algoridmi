# 🚂 Настройка Railway PostgreSQL

## ✅ Что Сделано

Платформа теперь поддерживает **Railway PostgreSQL** через `DATABASE_PUBLIC_URL`!

---

## 🎯 Быстрая Настройка

### Шаг 1: Добавьте DATABASE_PUBLIC_URL в .env

Откройте файл `.env` и добавьте вашу строку подключения:

```env
DATABASE_PUBLIC_URL=postgresql://postgres:VhkpNnpwtAXXIYEpmJPwJOiyNiykqvDN@switchback.proxy.rlwy.net:24216/railway
```

**Формат:**
```
postgresql://[user]:[password]@[host]:[port]/[database]
```

### Шаг 2: Установите Зависимости

```bash
pip install -r requirements.txt
```

Это установит:
- `psycopg2-binary` - драйвер PostgreSQL
- `dj-database-url` - парсер DATABASE_URL

### Шаг 3: Примените Миграции

```bash
python manage.py migrate
```

### Шаг 4: Создайте Суперпользователя

```bash
python manage.py createsuperuser
```

### Шаг 5: Запустите Сервер

```bash
python manage.py runserver
```

---

## 📋 Полный Пример .env

```env
# Django Settings
SECRET_KEY=django-insecure-dev-key-change-in-production
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1

# Railway PostgreSQL (приоритет)
DATABASE_PUBLIC_URL=postgresql://postgres:VhkpNnpwtAXXIYEpmJPwJOiyNiykqvDN@switchback.proxy.rlwy.net:24216/railway

# Google Gemini API
GEMINI_API_KEY=ваш-ключ-здесь

# CORS Settings
CORS_ALLOWED_ORIGINS=http://localhost:8000,http://127.0.0.1:8000

# JWT Settings
JWT_ACCESS_TOKEN_LIFETIME=60
JWT_REFRESH_TOKEN_LIFETIME=1440
```

---

## 🔧 Как Это Работает?

### Приоритет Подключения

Django проверяет настройки в следующем порядке:

1. **DATABASE_PUBLIC_URL** (Railway формат) ⭐
2. **DATABASE_URL** (Heroku формат)
3. **USE_POSTGRESQL=True** (отдельные параметры)
4. **SQLite** (по умолчанию)

### Код в settings.py

```python
import dj_database_url

DATABASE_PUBLIC_URL = config('DATABASE_PUBLIC_URL', default=None)

if DATABASE_PUBLIC_URL:
    DATABASES = {
        'default': dj_database_url.parse(DATABASE_PUBLIC_URL)
    }
```

---

## 🌐 Варианты Подключения

### Вариант 1: Railway (DATABASE_PUBLIC_URL)

```env
DATABASE_PUBLIC_URL=postgresql://user:password@host:port/database
```

**Преимущества:**
- ✅ Одна строка
- ✅ Легко копировать из Railway
- ✅ Работает с Railway, Heroku, Render

### Вариант 2: Отдельные Параметры

```env
USE_POSTGRESQL=True
DB_NAME=al_khwarizmi_db
DB_USER=postgres
DB_PASSWORD=your_password
DB_HOST=localhost
DB_PORT=5432
```

**Преимущества:**
- ✅ Более читаемо
- ✅ Легко менять отдельные параметры

### Вариант 3: SQLite (По умолчанию)

Если ничего не указано, используется SQLite:

```python
DATABASES = {
    'default': {
        'ENGINE': 'django.db.backends.sqlite3',
        'NAME': BASE_DIR / 'db.sqlite3',
    }
}
```

**Преимущества:**
- ✅ Не требует настройки
- ✅ Идеально для разработки
- ✅ Файл `db.sqlite3` в проекте

---

## 🚀 Деплой на Railway

### Шаг 1: Создайте Проект на Railway

1. Перейдите на https://railway.app/
2. Нажмите **"New Project"**
3. Выберите **"Deploy from GitHub repo"**
4. Выберите ваш репозиторий

### Шаг 2: Добавьте PostgreSQL

1. В проекте нажмите **"New"**
2. Выберите **"Database"**
3. Выберите **"PostgreSQL"**
4. Railway автоматически создаст БД

### Шаг 3: Скопируйте DATABASE_PUBLIC_URL

1. Откройте PostgreSQL сервис
2. Перейдите на вкладку **"Connect"**
3. Скопируйте **"Postgres Connection URL"**

### Шаг 4: Настройте Переменные Окружения

В настройках проекта добавьте:

```env
DATABASE_PUBLIC_URL=postgresql://postgres:...@...railway.app:5432/railway
GEMINI_API_KEY=ваш-ключ
SECRET_KEY=ваш-секретный-ключ
DEBUG=False
ALLOWED_HOSTS=.railway.app
```

### Шаг 5: Деплой

Railway автоматически:
- Установит зависимости из `requirements.txt`
- Запустит `python manage.py migrate`
- Запустит сервер

---

## 🔍 Проверка Подключения

### Тест 1: Django Shell

```bash
python manage.py shell
```

```python
from django.db import connection
print(connection.settings_dict)
# Должно показать параметры PostgreSQL
```

### Тест 2: Миграции

```bash
python manage.py showmigrations
```

Должны отображаться все миграции.

### Тест 3: Создание Данных

```bash
python manage.py createsuperuser
```

Если успешно создается - подключение работает!

---

## 🐛 Решение Проблем

### "Could not connect to server"

**Причина:** Неправильный URL или сервер недоступен

**Решение:**
1. Проверьте правильность `DATABASE_PUBLIC_URL`
2. Убедитесь, что PostgreSQL запущен на Railway
3. Проверьте firewall/сеть

### "FATAL: password authentication failed"

**Причина:** Неправильный пароль

**Решение:**
1. Скопируйте URL заново из Railway
2. Убедитесь, что нет лишних пробелов
3. Проверьте спецсимволы в пароле

### "relation does not exist"

**Причина:** Миграции не применены

**Решение:**
```bash
python manage.py migrate
```

### "ModuleNotFoundError: No module named 'dj_database_url'"

**Причина:** Не установлен пакет

**Решение:**
```bash
pip install dj-database-url
```

---

## 📊 Сравнение БД

| Параметр | SQLite | PostgreSQL (Railway) |
|----------|--------|---------------------|
| **Установка** | Не требуется | Требуется настройка |
| **Производительность** | Средняя | Высокая |
| **Одновременные пользователи** | До 10 | Тысячи |
| **Размер БД** | До 1GB | Неограничен |
| **Production** | ❌ Не рекомендуется | ✅ Рекомендуется |
| **Разработка** | ✅ Идеально | ✅ Хорошо |

---

## 🎯 Рекомендации

### Для Разработки

Используйте **SQLite** (по умолчанию):
- Не требует настройки
- Быстрый старт
- Легко сбросить БД

### Для Production

Используйте **PostgreSQL** (Railway):
- Высокая производительность
- Надежность
- Масштабируемость
- Бэкапы

### Для Тестирования

Используйте **отдельную БД**:
```env
DATABASE_PUBLIC_URL=postgresql://...test_database
```

---

## 🔐 Безопасность

### ⚠️ Важно!

1. **Не коммитьте .env в Git!**
   - Файл уже в `.gitignore`
   - Проверьте: `git status`

2. **Используйте сильные пароли**
   - Railway генерирует автоматически
   - Не меняйте без необходимости

3. **Ограничьте доступ**
   - Railway имеет встроенный firewall
   - Используйте VPN для дополнительной защиты

4. **Регулярные бэкапы**
   - Railway делает автоматически
   - Можно скачать вручную

---

## 📈 Мониторинг

### Railway Dashboard

1. **Метрики БД:**
   - CPU usage
   - Memory usage
   - Connections
   - Query time

2. **Логи:**
   - Все SQL запросы
   - Ошибки подключения
   - Slow queries

3. **Alerts:**
   - Настройте уведомления
   - Email/Slack интеграция

---

## 🎓 Полезные Команды

### Django Management

```bash
# Проверка подключения
python manage.py dbshell

# Сброс БД (осторожно!)
python manage.py flush

# Создание бэкапа
python manage.py dumpdata > backup.json

# Восстановление из бэкапа
python manage.py loaddata backup.json

# Просмотр SQL запросов
python manage.py sqlmigrate users 0001
```

### PostgreSQL Команды

```bash
# Подключение к БД
psql postgresql://user:password@host:port/database

# Список таблиц
\dt

# Описание таблицы
\d table_name

# Выход
\q
```

---

## ✅ Чеклист Настройки

- [ ] Создан проект на Railway
- [ ] Добавлен PostgreSQL сервис
- [ ] Скопирован DATABASE_PUBLIC_URL
- [ ] Добавлен в `.env` файл
- [ ] Установлен `dj-database-url`
- [ ] Применены миграции
- [ ] Создан суперпользователь
- [ ] Проверено подключение
- [ ] Загружены начальные данные
- [ ] Протестирована работа

---

## 🎉 Готово!

**Ваша платформа теперь использует Railway PostgreSQL!**

### Преимущества:

✅ **Облачная БД** - доступна отовсюду  
✅ **Автоматические бэкапы** - Railway делает сам  
✅ **Высокая производительность** - оптимизированный сервер  
✅ **Масштабируемость** - растет с вашим проектом  
✅ **Мониторинг** - встроенная аналитика  

### Следующие Шаги:

1. Примените миграции
2. Загрузите начальные данные
3. Создайте суперпользователя
4. Протестируйте работу
5. Настройте production окружение

---

**Удачи с Railway PostgreSQL! 🚂🐘**
