# 📚 Система Импорта Математических Задач из Книг

## 🎯 Что Создано

Полная система автоматического импорта математических задач из книг и сборников с использованием **Gemini AI** для интеллектуальной структуризации.

### ✨ Основные Компоненты

1. **`core/book_importer.py`** - Сервис импорта с Gemini AI
2. **`problems/views_book_import.py`** - API endpoints для загрузки книг
3. **`problems/management/commands/import_book.py`** - Команда для импорта
4. **`sample_problems.txt`** - Пример файла с задачами для тестирования

### 🚀 Возможности

- ✅ Импорт из **PDF** файлов
- ✅ Импорт из **TXT/MD** файлов
- ✅ **AI структуризация** через Gemini (условие, решение, ответ, подсказки)
- ✅ Автоматическое определение **темы, сложности, класса**
- ✅ Извлечение **LaTeX формул**
- ✅ Пакетная обработка целых книг
- ✅ Валидация и проверка качества
- ✅ Статистика импорта

---

## 📦 Установка

### 1. Зависимости

```bash
pip install google-generativeai pdfplumber
```

### 2. API Ключ

Получите Gemini API ключ: https://aistudio.google.com/app/apikey

Добавьте в `.env`:
```env
GEMINI_API_KEY=your_api_key_here
```

---

## 🎯 Быстрый Старт

### Тест с Примером

```bash
# 1. Импортируйте тестовый файл
python manage.py import_book sample_problems.txt \
    --topic "Алгебра: Базовый курс" \
    --grade 8

# 2. Проверьте результат в админке
python manage.py runserver
# Откройте http://localhost:8000/admin/problems/problem/
# Фильтр: Source = "imported"
```

### Импорт Своей Книги

```bash
python manage.py import_book "path/to/your/book.pdf" \
    --topic "Геометрия: Планиметрия" \
    --grade 9 \
    --min-difficulty 1000 \
    --max-difficulty 1500
```

---

## 📖 Использование

### Через Management Команду

```bash
# Базовый импорт
python manage.py import_book problems.txt

# С параметрами
python manage.py import_book algebra_book.pdf \
    --topic "Алгебра: Уравнения" \
    --grade 9 \
    --min-difficulty 1200 \
    --max-difficulty 1800
```

**Параметры:**
- `file_path` - путь к файлу (обязательно)
- `--topic` - название темы
- `--grade` - класс (1-12)
- `--min-difficulty` - минимальная сложность (по умолчанию 800)
- `--max-difficulty` - максимальная сложность (по умолчанию 1500)

### Через API (Админы)

#### Загрузка файла

```bash
curl -X POST \
  -H "Authorization: Bearer ADMIN_TOKEN" \
  -F "file=@algebra_problems.pdf" \
  -F "topic=Алгебра: Квадратные уравнения" \
  -F "grade=9" \
  http://localhost:8000/api/problems/import/book/
```

#### Импорт из текста

```bash
curl -X POST \
  -H "Authorization: Bearer ADMIN_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
    "text": "Задача 1: Решите x² = 4\nОтвет: x = ±2",
    "topic": "Алгебра",
    "grade": 8
  }' \
  http://localhost:8000/api/problems/import/text/
```

#### Статистика

```bash
curl -H "Authorization: Bearer ADMIN_TOKEN" \
  http://localhost:8000/api/problems/import/statistics/
```

---

## 📋 Формат Файлов

### Рекомендуемая Структура

```txt
Задача 1. [Название]
[Условие задачи]

Решение:
[Пошаговое решение]

Ответ: [Правильный ответ]

---

Задача 2. [Название]
...
```

### Примеры Форматов

#### Минимальный
```txt
1. Решите: 2x = 6
Ответ: x = 3
```

#### С решением
```txt
Задача: Найдите площадь круга радиусом 5 см.

Решение:
S = πr² = 3.14 × 5² = 3.14 × 25 = 78.5 см²

Ответ: 78.5 см²
```

#### С LaTeX
```txt
Задача: Вычислите $\int_0^1 x^2 dx$

Решение:
$\int x^2 dx = \frac{x^3}{3} + C$

Ответ: $\frac{1}{3}$
```

---

## 🤖 Как Работает AI

### 1. Извлечение
- PDF → Gemini Document Processing API
- TXT → Прямая передача

### 2. Структуризация
Gemini анализирует текст и извлекает:
- Номер и название задачи
- Полное условие
- LaTeX формулы
- Пошаговое решение
- Правильный ответ
- Подсказки
- Тему (раздел математики)
- Сложность (0-3000)
- Рекомендуемый класс (1-12)

### 3. Сохранение
- Валидация данных
- Создание/связывание темы
- Нормализация сложности
- Сохранение в БД

---

## 📊 API Endpoints

### POST `/api/problems/import/book/`
Загрузить и импортировать книгу (PDF/TXT)

**Form Data:**
- `file` - файл
- `topic` - тема (опционально)
- `grade` - класс (опционально)
- `min_difficulty` - мин. сложность
- `max_difficulty` - макс. сложность

**Ответ:**
```json
{
  "success": true,
  "message": "Импортировано 45 из 50 задач",
  "results": {
    "total": 50,
    "imported": 45,
    "skipped": 5,
    "errors": []
  }
}
```

### POST `/api/problems/import/text/`
Импортировать задачи из текста

**Body:**
```json
{
  "text": "текст с задачами",
  "topic": "Алгебра",
  "grade": 9
}
```

### GET `/api/problems/import/statistics/`
Статистика импортированных задач

**Ответ:**
```json
{
  "success": true,
  "statistics": {
    "total_imported": 450,
    "by_topic": [...],
    "by_grade": [...],
    "by_difficulty": [...]
  }
}
```

---

## 📈 Примеры Использования

### Пример 1: Импорт Учебника

```bash
# Импортируем учебник алгебры для 9 класса
python manage.py import_book "books/algebra_9_class.pdf" \
    --topic "Алгебра: 9 класс" \
    --grade 9 \
    --min-difficulty 1200 \
    --max-difficulty 1800
```

**Результат:**
```
📚 Начинаем импорт из: books/algebra_9_class.pdf
   Тема: Алгебра: 9 класс
   Класс: 9
   Диапазон сложности: 1200-1800

📄 Обработка PDF файла...

✅ Импорт завершен успешно!
   Всего задач найдено: 150
   Импортировано: 145
   Пропущено: 5
```

### Пример 2: Импорт Сборника

```bash
python manage.py import_book "collections/geometry_problems.txt" \
    --topic "Геометрия: Планиметрия" \
    --grade 8
```

### Пример 3: Проверка Импорта

```python
# Django shell
python manage.py shell

from problems.models import Problem

# Все импортированные задачи
imported = Problem.objects.filter(source='imported')
print(f"Импортировано: {imported.count()} задач")

# По теме
algebra = imported.filter(topic__name__contains='Алгебра')
for problem in algebra[:5]:
    print(f"- {problem.title} (сложность: {problem.difficulty_score})")
```

---

## 🎯 Best Practices

### Подготовка Файлов

✅ **DO:**
- Используйте PDF с текстовым слоем (не сканы)
- Структурируйте задачи последовательно
- Явно указывайте ответы
- Добавляйте решения для сложных задач

❌ **DON'T:**
- Не используйте отсканированные изображения
- Избегайте нестандартных форматов
- Не смешивайте разные темы в одном файле

### Параметры Импорта

✅ **Рекомендации:**
- Указывайте тему для лучшей категоризации
- Задавайте класс, если известен
- Используйте реалистичные диапазоны сложности
- Импортируйте порциями по 50-100 задач

### После Импорта

✅ **Проверьте:**
- Корректность условий и ответов
- LaTeX формулы
- Сложность задач
- Привязку к темам

---

## 🐛 Troubleshooting

### "GEMINI_API_KEY не установлен"
→ Добавьте ключ в `.env` файл

### "Не удалось извлечь задачи"
→ Проверьте формат файла, используйте стандартную структуру

### "Импортировано 0 задач"
→ Убедитесь, что файл содержит задачи с явными маркерами

### Низкое качество распознавания
→ Добавьте явные маркеры ("Задача:", "Ответ:")
→ Структурируйте текст последовательно
→ Укажите тему и класс

---

## 📚 Документация

- **`BOOK_IMPORT_GUIDE.md`** - Полное руководство
- **`QUICK_START_BOOK_IMPORT.md`** - Быстрый старт
- **`sample_problems.txt`** - Пример файла с задачами

---

## 🎉 Готово!

Система импорта задач из книг полностью настроена и готова к использованию!

**Следующие шаги:**
1. Протестируйте с `sample_problems.txt`
2. Импортируйте свои книги
3. Проверьте результаты в админке
4. Используйте задачи для генерации пользователям

**Поддержка:**
- Документация: `BOOK_IMPORT_GUIDE.md`
- Примеры: `sample_problems.txt`
- API: `/api/problems/import/`
