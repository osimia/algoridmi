# 📚 Руководство по Импорту Задач из Книг

## 🎯 Обзор

Система автоматического импорта математических задач из книг и сборников с использованием Gemini AI для структуризации.

### ✨ Возможности

- ✅ **Импорт из PDF** - автоматическое извлечение задач из PDF файлов
- ✅ **Импорт из текста** - обработка TXT, MD файлов
- ✅ **AI структуризация** - Gemini автоматически извлекает условие, решение, ответ
- ✅ **Автоматическая категоризация** - определение темы, сложности, класса
- ✅ **Пакетный импорт** - обработка целых книг за раз
- ✅ **Валидация** - проверка корректности импортированных данных

---

## 🔧 Установка Зависимостей

Добавьте в `requirements.txt`:

```txt
google-generativeai>=0.3.0
pdfplumber>=0.10.0
```

Установите:

```bash
pip install google-generativeai pdfplumber
```

---

## 📖 Использование

### 1. Через Management Команду

#### Импорт из PDF

```bash
python manage.py import_book "path/to/book.pdf" \
    --topic "Алгебра: Квадратные уравнения" \
    --grade 9 \
    --min-difficulty 1000 \
    --max-difficulty 1500
```

#### Импорт из TXT

```bash
python manage.py import_book "path/to/problems.txt" \
    --topic "Геометрия: Треугольники" \
    --grade 8
```

#### Параметры команды

- `file_path` - путь к файлу (обязательно)
- `--topic` - название темы (опционально)
- `--grade` - класс 1-12 (опционально)
- `--min-difficulty` - минимальная сложность (по умолчанию 800)
- `--max-difficulty` - максимальная сложность (по умолчанию 1500)

### 2. Через API (Админы)

#### Загрузка файла

```bash
curl -X POST \
  -H "Authorization: Bearer ADMIN_TOKEN" \
  -F "file=@algebra_problems.pdf" \
  -F "topic=Алгебра: Уравнения" \
  -F "grade=9" \
  -F "min_difficulty=1000" \
  -F "max_difficulty=1500" \
  http://localhost:8000/api/problems/import/book/
```

#### Импорт из текста

```bash
curl -X POST \
  -H "Authorization: Bearer ADMIN_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
    "text": "Задача 1: Решите уравнение x^2 - 5x + 6 = 0\nОтвет: x1=2, x2=3",
    "topic": "Алгебра: Квадратные уравнения",
    "grade": 8,
    "min_difficulty": 800,
    "max_difficulty": 1200
  }' \
  http://localhost:8000/api/problems/import/text/
```

#### Статистика импорта

```bash
curl -H "Authorization: Bearer ADMIN_TOKEN" \
  http://localhost:8000/api/problems/import/statistics/
```

---

## 📋 Формат Книг

### Рекомендуемая Структура

Для лучшего распознавания задач, книга должна иметь структуру:

```
Задача 1. [Название]
Условие: [текст условия]
Решение: [шаги решения]
Ответ: [правильный ответ]

Задача 2. [Название]
...
```

### Поддерживаемые Форматы

1. **PDF** - любые PDF файлы с текстом
2. **TXT** - простые текстовые файлы
3. **MD** - Markdown файлы

### Примеры Форматов

#### Формат 1: Классический

```
1. Решите уравнение: x² - 5x + 6 = 0

Решение:
Используем формулу корней квадратного уравнения.
D = b² - 4ac = 25 - 24 = 1
x₁ = (5 + 1)/2 = 3
x₂ = (5 - 1)/2 = 2

Ответ: x₁ = 3, x₂ = 2
```

#### Формат 2: Упрощенный

```
Задача: Найдите площадь треугольника со сторонами 3, 4, 5.
Ответ: 6
```

#### Формат 3: С LaTeX

```
Задача: Вычислите $\int_0^1 x^2 dx$

Решение:
$\int x^2 dx = \frac{x^3}{3} + C$
$\int_0^1 x^2 dx = \frac{1}{3} - 0 = \frac{1}{3}$

Ответ: $\frac{1}{3}$
```

---

## 🤖 Как Работает AI Структуризация

### 1. Извлечение Текста

- PDF → Gemini Document Processing API
- TXT → Прямая передача текста

### 2. AI Анализ

Gemini анализирует текст и извлекает:

- **Номер задачи** - порядковый номер
- **Название** - краткое описание
- **Условие** - полный текст задачи
- **LaTeX формулы** - математические выражения
- **Решение** - пошаговое решение
- **Ответ** - правильный ответ
- **Подсказки** - если есть
- **Тему** - раздел математики
- **Сложность** - оценка 0-3000
- **Класс** - рекомендуемый класс

### 3. Валидация и Сохранение

- Проверка обязательных полей
- Нормализация сложности
- Создание/связывание темы
- Сохранение в БД

---

## 📊 Примеры Использования

### Пример 1: Импорт Сборника Задач

```bash
# Импортируем сборник задач по алгебре для 9 класса
python manage.py import_book "books/algebra_9_class.pdf" \
    --topic "Алгебра: Общий курс" \
    --grade 9 \
    --min-difficulty 1200 \
    --max-difficulty 1800
```

**Результат:**
```
📚 Начинаем импорт из: books/algebra_9_class.pdf
   Тема: Алгебра: Общий курс
   Класс: 9
   Диапазон сложности: 1200-1800

📄 Обработка PDF файла...

✅ Импорт завершен успешно!
   Всего задач найдено: 150
   Импортировано: 145
   Пропущено: 5
```

### Пример 2: Импорт из Текстового Файла

```bash
python manage.py import_book "problems/geometry_triangles.txt" \
    --topic "Геометрия: Треугольники" \
    --grade 8
```

### Пример 3: Импорт Через API

```python
import requests

# Загружаем файл
with open('algebra_book.pdf', 'rb') as f:
    files = {'file': f}
    data = {
        'topic': 'Алгебра: Уравнения',
        'grade': 9,
        'min_difficulty': 1000,
        'max_difficulty': 1500
    }
    
    response = requests.post(
        'http://localhost:8000/api/problems/import/book/',
        files=files,
        data=data,
        headers={'Authorization': 'Bearer YOUR_ADMIN_TOKEN'}
    )
    
    print(response.json())
```

**Ответ:**
```json
{
  "success": true,
  "message": "Импортировано 45 из 50 задач",
  "results": {
    "total": 50,
    "imported": 45,
    "skipped": 5,
    "errors": [
      {
        "problem_number": 12,
        "error": "Не удалось извлечь ответ"
      }
    ]
  }
}
```

---

## 📈 Статистика Импорта

### Получение Статистики

```bash
curl -H "Authorization: Bearer ADMIN_TOKEN" \
  http://localhost:8000/api/problems/import/statistics/
```

**Ответ:**
```json
{
  "success": true,
  "statistics": {
    "total_imported": 450,
    "by_topic": [
      {"topic__name": "Алгебра: Уравнения", "count": 120},
      {"topic__name": "Геометрия: Треугольники", "count": 85},
      {"topic__name": "Алгебра: Неравенства", "count": 75}
    ],
    "by_grade": [
      {"grade_level": 7, "count": 50},
      {"grade_level": 8, "count": 120},
      {"grade_level": 9, "count": 180},
      {"grade_level": 10, "count": 100}
    ],
    "by_difficulty": [
      {"range": "Легкие (0-800)", "count": 80},
      {"range": "Средние (800-1200)", "count": 200},
      {"range": "Сложные (1200-1600)", "count": 150},
      {"range": "Очень сложные (1600+)", "count": 20}
    ]
  }
}
```

---

## 🎯 Best Practices

### 1. Подготовка Файлов

- ✅ Используйте качественные PDF с текстовым слоем
- ✅ Избегайте отсканированных изображений (OCR не поддерживается)
- ✅ Структурируйте задачи последовательно
- ✅ Указывайте ответы явно

### 2. Параметры Импорта

- ✅ Указывайте тему для лучшей категоризации
- ✅ Задавайте класс, если известен
- ✅ Используйте реалистичные диапазоны сложности
- ✅ Импортируйте небольшими порциями (50-100 задач)

### 3. После Импорта

- ✅ Проверьте импортированные задачи в админке
- ✅ Исправьте ошибки вручную при необходимости
- ✅ Добавьте недостающие подсказки
- ✅ Проверьте LaTeX формулы

---

## 🐛 Troubleshooting

### Ошибка: "GEMINI_API_KEY не установлен"

Добавьте в `.env`:
```
GEMINI_API_KEY=your_api_key_here
```

### Ошибка: "Не удалось извлечь задачи"

**Причины:**
- PDF содержит только изображения (нужен OCR)
- Неструктурированный текст
- Нестандартный формат задач

**Решение:**
- Используйте PDF с текстовым слоем
- Переформатируйте текст в стандартный вид
- Попробуйте импорт из TXT

### Низкое качество распознавания

**Улучшение:**
- Добавьте явные маркеры ("Задача:", "Ответ:")
- Структурируйте текст последовательно
- Разбейте большой файл на части
- Укажите тему и класс явно

---

## 📚 Рекомендуемые Источники

### Популярные Сборники Задач

1. **Алгебра**
   - Мордкович А.Г. - Задачник по алгебре
   - Макарычев Ю.Н. - Дидактические материалы

2. **Геометрия**
   - Атанасян Л.С. - Задачи по геометрии
   - Погорелов А.В. - Сборник задач

3. **Олимпиадные задачи**
   - Всероссийская олимпиада школьников
   - Математические кружки

### Онлайн Источники

- **Project Euler** - задачи по программированию и математике
- **Brilliant.org** - интерактивные задачи
- **AoPS** - Art of Problem Solving

---

## 🔒 Безопасность

### Права Доступа

- ❌ Обычные пользователи НЕ могут импортировать
- ✅ Только администраторы имеют доступ
- ✅ Все импорты логируются

### Ограничения

- Максимальный размер файла: 50MB
- Максимум задач за раз: 500
- Rate limit: 10 импортов в час

---

## 🎉 Готово!

Система импорта задач из книг полностью настроена!

**Следующие шаги:**
1. Подготовьте PDF/TXT файлы с задачами
2. Запустите импорт через команду или API
3. Проверьте результаты в админке
4. Задачи готовы для использования!

**Документация:**
- `BOOK_IMPORT_GUIDE.md` - это руководство
- `core/book_importer.py` - код импортера
- `problems/views_book_import.py` - API endpoints
