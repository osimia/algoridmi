# 🔧 Применение миграций для системы задач

## ⚠️ Важно!

Перед запуском команды генерации задач нужно применить миграции к БД.

## 📝 Шаги:

### 1. Убедитесь что виртуальное окружение активировано:

```bash
# Должно быть (venv) в начале строки
(venv) PS C:\Users\Hp\OneDrive\Desktop\algoritm>
```

Если нет, активируйте:
```bash
venv\Scripts\activate
```

### 2. Примените миграции:

```bash
python manage.py migrate problems
```

Вы должны увидеть:
```
Running migrations:
  Applying problems.0003_problem_grade_level_problem_source_and_more... OK
  Applying problems.0004_alter_problem_topic... OK
```

### 3. Теперь можно генерировать задачи:

```bash
# Генерация 10 задач для 5 класса
python manage.py generate_problems_bulk --count 10 --grade 5
```

## ✅ Готово!

После применения миграций поле `topic` стало опциональным и задачи будут сохраняться без ошибок.

---

**Примечание:** Миграции уже созданы автоматически:
- `0003_problem_grade_level_problem_source_and_more.py` - Добавляет новые поля
- `0004_alter_problem_topic.py` - Делает поле topic опциональным
