# 🚀 Руководство по массовой генерации задач

## 📋 Описание

Созданы 3 скрипта для массовой генерации задач:
- `generate_all_problems.ps1` - для Windows (PowerShell)
- `generate_all_problems.sh` - для Linux/Mac (Bash)
- `generate_all_problems.py` - кросс-платформенный (Python)

## 📊 Что генерируется

### Часть 1: Школьные классы (1-12)
- **По 10 задач** для каждого класса
- **Всего: 120 задач**

| Класс | Количество задач |
|-------|------------------|
| 1     | 10               |
| 2     | 10               |
| 3     | 10               |
| ...   | ...              |
| 12    | 10               |

### Часть 2: Уровни сложности (вне школьной программы)
- **По 10 задач** для каждого уровня
- **Всего: 70 задач**

| Уровень | Сложность | Количество задач |
|---------|-----------|------------------|
| Начальный | 0-300 | 10 |
| Легкий | 300-600 | 10 |
| Средний | 600-900 | 10 |
| Выше среднего | 900-1200 | 10 |
| Сложный | 1200-1500 | 10 |
| Очень сложный | 1500-2000 | 10 |
| Экспертный | 2000-3000 | 10 |

### 📈 Итого
**190 задач** за один запуск!

## 🔧 Использование

### Windows (PowerShell):

```powershell
# Запуск скрипта
.\generate_all_problems.ps1
```

Если возникает ошибка политики выполнения:
```powershell
# Разрешить выполнение скриптов (один раз)
Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser

# Затем запустить
.\generate_all_problems.ps1
```

### Linux/Mac (Bash):

```bash
# Сделать скрипт исполняемым (один раз)
chmod +x generate_all_problems.sh

# Запуск
./generate_all_problems.sh
```

### Кросс-платформенный (Python):

```bash
# Windows
python generate_all_problems.py

# Linux/Mac
python3 generate_all_problems.py
```

## ⏱️ Время выполнения

При настройках по умолчанию (задержка 2 сек между запросами):

- **Школьные классы (120 задач):** ~40-60 минут
- **Уровни сложности (70 задач):** ~25-35 минут
- **Всего:** ~65-95 минут (1-1.5 часа)

### Ускорение генерации

Можно уменьшить задержку, но это может привести к ошибкам API:

**В PowerShell скрипте:**
```powershell
# Найти строку
$delay = 2.0

# Изменить на
$delay = 1.0  # Быстрее, но рискованнее
```

**В Python скрипте:**
```python
# Найти строку
delay = 2.0

# Изменить на
delay = 1.0  # Быстрее, но рискованнее
```

## 📊 Вывод скрипта

Скрипт показывает прогресс в реальном времени:

```
========================================
🚀 МАССОВАЯ ГЕНЕРАЦИЯ ЗАДАЧ
========================================

📊 Параметры генерации:
   - Задач на класс: 10
   - Задержка: 2.0 сек

📚 ЧАСТЬ 1: Генерация задач для школьных классов (1-12)
========================================

📝 Класс 1...
   Тема: Арифметика: Сложение и вычитание
   Класс: 1
   Сложность: 150
   ✅ Задача сохранена: ID=1
   ...
   ✅ Успешно сгенерировано: 10 задач

📝 Класс 2...
   ...

🎯 ЧАСТЬ 2: Генерация задач для разных уровней сложности
========================================

🎓 Уровень: Начальный (0-300)
   ...

========================================
🎉 ГЕНЕРАЦИЯ ЗАВЕРШЕНА!
========================================

📊 Статистика:
   - Всего задач запланировано: 190
   - Успешно сгенерировано: 185
   - Ошибок: 5

📚 Проверяем БД...
Всего задач в БД: 185

========================================
✅ Готово!
========================================
```

## ⚠️ Возможные проблемы

### 1. Ошибки API (Rate Limit)

**Проблема:** Слишком много запросов к Gemini API

**Решение:**
- Увеличить задержку между запросами
- Запустить скрипт позже
- Разбить генерацию на несколько сессий

### 2. Ошибки валидации

**Проблема:** AI генерирует некорректные задачи

**Решение:**
- Задачи автоматически регенерируются
- Если ошибка повторяется, задача пропускается
- Можно проверить и отредактировать в админке

### 3. Прерывание скрипта

**Проблема:** Скрипт прерван (Ctrl+C)

**Решение:**
- Уже сгенерированные задачи сохранены в БД
- Можно запустить скрипт снова
- Дубликаты не создадутся (разные задачи каждый раз)

## 🔍 Проверка результатов

### В админ-панели:

1. Перейти в админку: http://127.0.0.1:8000/admin/
2. Раздел "Задачи" → "Problems"
3. Фильтры:
   - По классу (1-12)
   - По источнику (AI сгенерирована)
   - По сложности

### В Django shell:

```python
python manage.py shell

# В shell:
from problems.models import Problem

# Всего задач
Problem.objects.count()

# По классам
for grade in range(1, 13):
    count = Problem.objects.filter(grade_level=grade).count()
    print(f"Класс {grade}: {count} задач")

# По уровням сложности
levels = [
    (0, 300, "Начальный"),
    (300, 600, "Легкий"),
    (600, 900, "Средний"),
    (900, 1200, "Выше среднего"),
    (1200, 1500, "Сложный"),
    (1500, 2000, "Очень сложный"),
    (2000, 3000, "Экспертный")
]

for min_d, max_d, name in levels:
    count = Problem.objects.filter(
        difficulty_score__gte=min_d,
        difficulty_score__lt=max_d
    ).count()
    print(f"{name} ({min_d}-{max_d}): {count} задач")
```

## 📝 Настройка параметров

### Изменить количество задач на класс:

**PowerShell:**
```powershell
$problemsPerGrade = 20  # Вместо 10
```

**Python:**
```python
problems_per_grade = 20  # Вместо 10
```

### Изменить количество задач по уровням:

**PowerShell:**
```powershell
@{Name="Начальный"; Min=0; Max=300; Count=20}  # Вместо 10
```

**Python:**
```python
{"name": "Начальный", "min": 0, "max": 300, "count": 20}  # Вместо 10
```

## 🎯 Рекомендации

### Первый запуск:
```bash
# Начните с малого количества для теста
# Измените в скрипте:
problems_per_grade = 2  # Вместо 10
count = 2  # Для каждого уровня
```

### Регулярное пополнение:
```bash
# Запускайте раз в неделю для пополнения базы
# Можно добавить в планировщик задач (Task Scheduler на Windows)
```

### Мониторинг:
```bash
# Проверяйте логи Django
# Следите за использованием API квоты Gemini
```

## 📚 Дополнительные команды

### Генерация только для одного класса:
```bash
python manage.py generate_problems_bulk --count 50 --grade 7
```

### Генерация только для одного уровня:
```bash
python manage.py generate_problems_bulk --count 30 --difficulty-min 1000 --difficulty-max 1500
```

### Генерация с увеличенной задержкой:
```bash
python manage.py generate_problems_bulk --count 10 --grade 5 --delay 5.0
```

---

**Дата создания:** 27 декабря 2024  
**Версия:** 1.0.0  
**Статус:** ✅ Готово к использованию
