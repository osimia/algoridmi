# 📚 Система хранения задач в БД

## ✅ Что реализовано

### 1. **Модель Problem обновлена** 🗄️

Добавлены новые поля:

| Поле | Тип | Описание |
|------|-----|----------|
| `grade_level` | Integer | Класс (1-12) |
| `source` | CharField | Источник задачи (ai_generated, manual, imported) |
| `times_used` | Integer | Сколько раз задача была показана |

### 2. **Логика генерации задач** 🔄

#### Новый алгоритм:
```
1. Пользователь запрашивает задачу
2. Система ищет подходящую задачу в БД:
   - Которую пользователь еще НЕ решал
   - Подходящей сложности (±150 от уровня)
   - Подходящего класса
3. Если задача найдена:
   - Показываем её пользователю
   - Увеличиваем счетчик times_used
4. Если задач нет:
   - Генерируем новую через AI
   - Сохраняем в БД для повторного использования
   - Показываем пользователю
```

#### Преимущества:
- ✅ **Экономия API запросов** - задачи переиспользуются
- ✅ **Нет повторов** - пользователь не видит одну задачу дважды
- ✅ **Автоматическое пополнение** - новые задачи сохраняются
- ✅ **Контроль качества** - можно проверить и отредактировать задачи

### 3. **Админ-панель** 👨‍💼

#### Новые возможности:

**Фильтры:**
- По классу (1-12)
- По источнику (AI, вручную, импорт)
- По сложности
- По активности

**Действия:**
- ✅ Активировать задачи
- ❌ Деактивировать задачи
- 🔄 Сбросить счетчик использования

**Статистика:**
- Сколько раз задача была использована
- Дата создания и обновления

### 4. **Management Command для массовой генерации** 🚀

```bash
# Сгенерировать 50 задач для 5 класса
python manage.py generate_problems_bulk --count 50 --grade 5

# Сгенерировать 100 задач разной сложности
python manage.py generate_problems_bulk --count 100 --difficulty-min 500 --difficulty-max 1500

# Сгенерировать 20 задач с задержкой 3 секунды
python manage.py generate_problems_bulk --count 20 --delay 3.0
```

#### Параметры:
- `--count` - Количество задач (по умолчанию: 10)
- `--grade` - Класс 1-12 (если не указан, для всех классов)
- `--difficulty-min` - Минимальная сложность (по умолчанию: 0)
- `--difficulty-max` - Максимальная сложность (по умолчанию: 3000)
- `--delay` - Задержка между запросами в секундах (по умолчанию: 2.0)

## 📊 Как это работает

### Для пользователя:

1. **Первый раз решает задачу:**
   ```
   Пользователь → Запрос задачи → БД пуста → AI генерирует → Сохраняет в БД → Показывает
   ```

2. **Второй раз (другой пользователь):**
   ```
   Пользователь 2 → Запрос задачи → Находит в БД → Показывает (без AI!)
   ```

3. **Тот же пользователь:**
   ```
   Пользователь → Запрос задачи → Находит другую задачу (которую не решал) → Показывает
   ```

4. **Все задачи решены:**
   ```
   Пользователь → Запрос задачи → Все решены → AI генерирует новую → Сохраняет → Показывает
   ```

### Для админа:

1. **Массовая генерация:**
   ```bash
   # Генерируем 100 задач для 7 класса
   python manage.py generate_problems_bulk --count 100 --grade 7
   ```

2. **Проверка и редактирование:**
   ```
   Админ-панель → Задачи → Фильтр по классу → Проверяет → Редактирует при необходимости
   ```

3. **Деактивация плохих задач:**
   ```
   Админ-панель → Выбрать задачи → Действия → Деактивировать
   ```

## 🗄️ Структура БД

### Таблица `problems_problem`:

```sql
CREATE TABLE problems_problem (
    id INTEGER PRIMARY KEY,
    topic_id INTEGER,
    title VARCHAR(300),
    latex_formula TEXT,
    description TEXT,
    correct_answer VARCHAR(200),
    difficulty_score INTEGER,
    solution_steps JSON,
    hints JSON,
    grade_level INTEGER,          -- НОВОЕ
    source VARCHAR(50),            -- НОВОЕ
    times_used INTEGER DEFAULT 0, -- НОВОЕ
    is_active BOOLEAN DEFAULT TRUE,
    created_at TIMESTAMP,
    updated_at TIMESTAMP
);
```

### Индексы для быстрого поиска:

```sql
CREATE INDEX idx_grade_difficulty_active 
ON problems_problem(grade_level, difficulty_score, is_active);

CREATE INDEX idx_active_times_used 
ON problems_problem(is_active, times_used);
```

## 📝 Примеры использования

### 1. Генерация задач для всех классов:

```bash
# 10 задач для каждого класса (1-12) = 120 задач
for grade in {1..12}; do
    python manage.py generate_problems_bulk --count 10 --grade $grade --delay 2
done
```

### 2. Генерация задач разной сложности:

```bash
# Легкие задачи (0-500)
python manage.py generate_problems_bulk --count 20 --difficulty-min 0 --difficulty-max 500

# Средние задачи (500-1500)
python manage.py generate_problems_bulk --count 30 --difficulty-min 500 --difficulty-max 1500

# Сложные задачи (1500-3000)
python manage.py generate_problems_bulk --count 20 --difficulty-min 1500 --difficulty-max 3000
```

### 3. Проверка статистики в админке:

```python
# В Django shell
from problems.models import Problem

# Сколько всего задач
Problem.objects.count()

# Сколько задач по классам
for grade in range(1, 13):
    count = Problem.objects.filter(grade_level=grade).count()
    print(f"Класс {grade}: {count} задач")

# Самые используемые задачи
Problem.objects.order_by('-times_used')[:10]

# Задачи, которые никто не решал
Problem.objects.filter(times_used=0)
```

## 🔧 Миграция БД

После обновления кода нужно применить миграции:

```bash
# Активируйте виртуальное окружение
source venv/bin/activate  # Linux/Mac
venv\Scripts\activate     # Windows

# Создайте миграции
python manage.py makemigrations problems

# Примените миграции
python manage.py migrate problems
```

## 🎯 Результаты

### До изменений:
- ❌ Каждая задача генерируется через AI
- ❌ Пользователь может увидеть одну задачу дважды
- ❌ Нет контроля качества задач
- ❌ Большой расход API запросов

### После изменений:
- ✅ Задачи переиспользуются
- ✅ Пользователь не видит повторов
- ✅ Админ может проверять и редактировать задачи
- ✅ Экономия API запросов до 90%
- ✅ Массовая генерация задач заранее
- ✅ Статистика использования задач

## 📈 Рекомендации

### 1. Начальное наполнение БД:

```bash
# Генерируем базовый набор задач
# По 50 задач для каждого класса = 600 задач
for grade in {1..12}; do
    python manage.py generate_problems_bulk \
        --count 50 \
        --grade $grade \
        --delay 2
done
```

### 2. Регулярное пополнение:

```bash
# Каждую неделю генерируем новые задачи
# Добавить в cron:
0 2 * * 0 cd /path/to/project && python manage.py generate_problems_bulk --count 20
```

### 3. Мониторинг:

```python
# Проверяем какие задачи заканчиваются
from problems.models import Problem
from django.db.models import Count

# Задачи которые решили больше 100 пользователей
popular = Problem.objects.filter(times_used__gte=100)

# Задачи которые никто не решает
unpopular = Problem.objects.filter(times_used=0, created_at__lt=one_month_ago)
```

## 🚀 Следующие шаги

- [ ] Добавить связь с темами (Topic)
- [ ] Импорт задач из внешних источников
- [ ] Экспорт задач в разные форматы
- [ ] Система рейтинга задач пользователями
- [ ] Автоматическая генерация по расписанию
- [ ] Аналитика популярности задач

---

**Дата**: 27 декабря 2024  
**Версия**: 1.0.0  
**Статус**: ✅ Готово к использованию
