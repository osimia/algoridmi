# 🔧 Исправление структуры задач - скрытие формул решения

## 🎯 Проблема

**До исправления:**
- Пользователю показывалась формула решения (`latex_formula`) сразу при получении задачи
- Это давало готовое решение и лишало смысла решать задачу самостоятельно
- Пример: задача на движение показывала формулу `S = V · T` до того, как пользователь начал решать

## ✅ Решение

### 1. Новая структура JSON от Gemini

**Было:**
```json
{
  "title": "Название",
  "latex_formula": "S = V \\cdot T",  // ❌ Показывалось сразу
  "description": "Описание",
  "correct_answer": "120",
  "solution_steps": [...],
  "hints": [...]
}
```

**Стало:**
```json
{
  "title": "Название",
  "problem_text": "Полный текст задачи БЕЗ формул решения",
  "description": "Описание задачи и что нужно найти",
  "equation_to_solve": "x + 5 = 12",  // ✅ Уравнение ИЗ условия (если есть)
  "solution_formula": "S = V \\cdot T",  // ✅ НЕ отправляется до проверки
  "correct_answer": "120",
  "solution_steps": [...],
  "hints": ["Подсказка БЕЗ формул", ...]
}
```

### 2. Разделение полей

| Поле | Назначение | Показывать пользователю |
|------|-----------|------------------------|
| `problem_text` | Полный текст условия задачи | ✅ ДА (сразу) |
| `description` | Описание что нужно найти | ✅ ДА (сразу) |
| `equation_to_solve` | Уравнение из условия задачи | ✅ ДА (сразу) |
| `solution_formula` | Формула ДЛЯ решения (S=V·T) | ❌ НЕТ (только после проверки) |
| `correct_answer` | Правильный ответ | ❌ НЕТ (только после проверки) |
| `solution_steps` | Пошаговое решение | ❌ НЕТ (только после проверки) |
| `hints` | Подсказки БЕЗ формул | ✅ ДА (по запросу) |

### 3. Обновленный промпт для Gemini

```
ВАЖНО О СТРУКТУРЕ:
- problem_text: ТОЛЬКО условие задачи, БЕЗ формул решения
- equation_to_solve: Уравнение ИЗ условия задачи (то что дано)
- solution_formula: Формула ДЛЯ решения (S=V·T и т.п.) - НЕ показывать до проверки
- hints: Подсказки БЕЗ готовых формул, только направление
```

### 4. API изменения

**Endpoint:** `GET /api/problems/generate-ai/`

**Ответ ДО проверки:**
```json
{
  "problem": {
    "id": "problem_123_...",
    "title": "Встреча на трассе с остановкой",
    "problem_text": "Два друга, Артем и Борис, решили встретиться...",
    "description": "Найти расстояние от города А",
    "equation_to_solve": "",  // Пусто для текстовых задач
    "difficulty_score": 867,
    "hints": ["Подумай о времени движения", "Учти остановку"],
    "generated_by_ai": true
    // НЕТ: solution_formula, correct_answer, solution_steps
  }
}
```

**Endpoint:** `POST /api/problems/submit-ai/`

**Ответ ПОСЛЕ проверки:**
```json
{
  "is_correct": true,
  "points_awarded": 150,
  "correct_answer": "120 км",
  "solution_formula": "S = V \\cdot T",  // ✅ Теперь показываем
  "equation_to_solve": "",
  "solution_steps": [
    "Шаг 1: Определяем время движения...",
    "Шаг 2: Применяем формулу S = V · T...",
    "..."
  ],
  "ai_feedback": "Отлично!",
  "confidence": 0.95
}
```

## 🔄 Что изменилось в коде

### 1. `core/gemini_service.py`

**Промпт обновлен:**
- Добавлено поле `problem_text` - только условие
- Добавлено поле `equation_to_solve` - уравнение из условия
- Добавлено поле `solution_formula` - формула для решения (скрыта)
- Удалено поле `latex_formula` (было двусмысленным)

**Валидация обновлена:**
```python
required_fields = [
    'title', 'problem_text', 'description',
    'correct_answer', 'solution_steps', 'hints'
]
```

### 2. `problems/views_gemini.py`

**Генерация задачи (строки 119-138):**
```python
response_data = {
    'problem': {
        'id': cache_key,
        'title': problem_data['title'],
        'problem_text': problem_data.get('problem_text', ''),
        'description': problem_data['description'],
        'equation_to_solve': problem_data.get('equation_to_solve', ''),
        'hints': problem_data.get('hints', []),
        # НЕ отправляем: solution_formula, correct_answer, solution_steps
    }
}
```

**Проверка ответа (строки 270-283):**
```python
response_data = {
    'is_correct': is_correct,
    'correct_answer': problem_data['correct_answer'],
    'solution_formula': problem_data.get('solution_formula', ''),  # Теперь показываем
    'solution_steps': problem_data.get('solution_steps', []),
    # ...
}
```

## 📊 Примеры

### Пример 1: Задача на движение

**Что видит пользователь ДО решения:**
```
Встреча на трассе с остановкой

Два друга, Артем и Борис, решили встретиться. Артем выехал на 
велосипеде из города А, а Борис — из города Б навстречу друг другу. 
Расстояние между городами А и Б составляет 180 км. Скорость Артема — 
20 км/ч, а скорость Бориса — 16 км/ч. Через 2 часа после старта Артем 
сделал остановку на 30 минут. После перерыва Артем продолжил движение 
с прежней скоростью. На каком расстоянии от города А друзья встретятся?

[Поле для ввода ответа]
```

**Что видит пользователь ПОСЛЕ проверки:**
```
✅ Правильно! +150 очков

Правильный ответ: 120 км

Формула для решения: S = V · T

Пошаговое решение:
1. Определяем время движения Артема до остановки: 2 часа
2. Расстояние Артема до остановки: S₁ = 20 · 2 = 40 км
3. Время остановки: 0.5 часа
4. За это время Борис проехал: S₂ = 16 · 2.5 = 40 км
...
```

### Пример 2: Иррациональное уравнение

**Что видит пользователь ДО решения:**
```
Решение иррационального уравнения

Найдите все действительные корни уравнения:
√(x+5) = x-1

Подсказки:
- Определите область допустимых значений
- После возведения в квадрат проверьте корни

[Поле для ввода ответа]
```

**Что видит пользователь ПОСЛЕ проверки:**
```
✅ Правильно! +150 очков

Правильный ответ: x = 4

Пошаговое решение:
1. Определяем ОДЗ: x+5 ≥ 0 и x-1 ≥ 0, откуда x ≥ 1
2. Возводим обе части в квадрат: x+5 = (x-1)²
3. Раскрываем: x+5 = x² - 2x + 1
4. Приводим к стандартному виду: x² - 3x - 4 = 0
5. Решаем: x₁ = 4, x₂ = -1
6. Проверяем ОДЗ: x = -1 не подходит (< 1)
7. Проверяем подстановкой x = 4: √9 = 3 ✓

Ответ: x = 4
```

## ✅ Преимущества нового подхода

1. **Образовательная ценность** - пользователь решает сам, без готовых формул
2. **Правильная структура** - четкое разделение условия и решения
3. **Гибкость** - можно показывать подсказки без раскрытия решения
4. **Безопасность** - решение хранится в кеше, но не отправляется
5. **Совместимость** - старые задачи из БД продолжают работать

## 🔄 Обратная совместимость

Для задач из БД (модель `Problem`) используется старое поле `latex_formula`.
Для AI-генерируемых задач используется новая структура.

```python
# В views_gemini.py
'problem_text': problem_data.get('problem_text', problem_data.get('description', ''))
'equation_to_solve': problem_data.get('equation_to_solve', '')
```

## 🧪 Тестирование

### Проверка что формула не показывается:

1. Сгенерировать задачу: `GET /api/problems/generate-ai/`
2. Проверить ответ - НЕ должно быть полей:
   - `solution_formula`
   - `correct_answer`
   - `solution_steps`
3. Должны быть только:
   - `problem_text`
   - `description`
   - `equation_to_solve` (если есть)
   - `hints`

### Проверка что формула показывается после решения:

1. Отправить ответ: `POST /api/problems/submit-ai/`
2. Проверить ответ - ДОЛЖНЫ быть поля:
   - `solution_formula`
   - `correct_answer`
   - `solution_steps`

## 📝 Миграция существующих задач

Для задач в БД (модель `Problem`):
- Поле `latex_formula` остается как есть
- При отображении используется как `equation_to_solve`
- Формулы решения добавляются в `solution_steps`

---

**Дата:** 26 декабря 2024  
**Версия:** 2.0.0  
**Статус:** ✅ Исправлено
