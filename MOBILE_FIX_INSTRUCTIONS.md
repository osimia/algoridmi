# 📱 Исправление Доступа к Профилю и Рейтингу на Мобильных

## 🔍 Проблема

На телефоне (через боевой домен) не открываются страницы:
- ❌ Профиль (`/api/user/profile/`)
- ❌ Рейтинг (`/api/arena/leaderboard/`)

При этом остальной сайт работает нормально.

## 🎯 Причина

Проблема с **cookies сессий** на мобильных браузерах при использовании HTTPS. Мобильные браузеры более строго относятся к настройкам безопасности cookies.

## ✅ Исправления

### 1. Настройки Сессий для HTTPS

```python
# settings.py
SESSION_COOKIE_SAMESITE = 'Lax'  # Lax для работы на мобильных
SESSION_COOKIE_SECURE = not DEBUG  # True для HTTPS в продакшене
```

**Что изменилось:**
- `SESSION_COOKIE_SECURE = True` для HTTPS (продакшен)
- `SESSION_COOKIE_SAMESITE = 'Lax'` вместо 'None' (совместимость с мобильными)

### 2. Настройки CSRF для HTTPS

```python
# settings.py
CSRF_COOKIE_SAMESITE = 'Lax'
CSRF_COOKIE_SECURE = not DEBUG  # True для HTTPS в продакшене
```

### 3. ALLOWED_HOSTS

```python
# settings.py
ALLOWED_HOSTS = config('ALLOWED_HOSTS', default='*').split(',') if DEBUG else config('ALLOWED_HOSTS', default='localhost,127.0.0.1').split(',')
```

### 4. Логирование для Отладки

Добавлено логирование в:
- `arena/views.py` - leaderboard
- `users/views.py` - get_current_user

Теперь можно видеть в логах:
```
INFO: Leaderboard request from user: username, authenticated: True
INFO: Profile request from user: username, authenticated: True
```

## 🚀 Применение Изменений

### Шаг 1: Коммит и Деплой

Если вы используете Railway/Heroku/другой хостинг:

```bash
git add .
git commit -m "Fix mobile access to profile and leaderboard"
git push origin main
```

### Шаг 2: Проверка Переменных Окружения

Убедитесь, что на сервере установлены правильные переменные:

```env
DEBUG=False
ALLOWED_HOSTS=ваш-домен.com
CORS_ALLOWED_ORIGINS=https://ваш-домен.com
```

### Шаг 3: Перезапуск Сервера

После деплоя сервер перезапустится автоматически.

## 🔍 Проверка Логов

Если проблема остается, проверьте логи на сервере:

```bash
# Railway
railway logs

# Heroku
heroku logs --tail
```

Ищите строки:
```
INFO: Profile request from user: ...
ERROR: User ... has no profile
```

## 📱 Проверка на Телефоне

### 1. Очистите Кэш Браузера

На телефоне:
- Chrome: Настройки → Конфиденциальность → Очистить данные
- Safari: Настройки → Safari → Очистить историю

### 2. Проверьте Cookies

Убедитесь, что браузер принимает cookies:
- Настройки → Конфиденциальность → Разрешить cookies

### 3. Попробуйте Войти Заново

1. Выйдите из аккаунта
2. Очистите кэш
3. Войдите снова
4. Попробуйте зайти в профиль и рейтинг

## 🔧 Дополнительные Проверки

### Проверка 1: HTTPS

Убедитесь, что сайт использует HTTPS:
```
https://ваш-домен.com ✅
http://ваш-домен.com ❌
```

### Проверка 2: Домен в CSRF_TRUSTED_ORIGINS

В `settings.py` должно быть:
```python
CSRF_TRUSTED_ORIGINS = [
    'https://ваш-домен.com',
]
```

### Проверка 3: Аутентификация

Проверьте, что пользователь авторизован:
```javascript
// В консоли браузера
fetch('/api/user/profile/', {
    credentials: 'include'
}).then(r => r.json()).then(console.log)
```

Должен вернуться профиль пользователя.

## 🎯 Возможные Дополнительные Проблемы

### Проблема: "CSRF token missing"

**Решение:**
```python
# settings.py
CSRF_COOKIE_HTTPONLY = False
```

### Проблема: "Session cookie not set"

**Решение:**
```python
# settings.py
SESSION_COOKIE_SAMESITE = 'Lax'  # НЕ 'Strict' или 'None'
SESSION_COOKIE_SECURE = True  # Для HTTPS
```

### Проблема: "CORS error"

**Решение:**
```python
# settings.py
CORS_ALLOW_CREDENTIALS = True
CORS_ALLOWED_ORIGINS = [
    'https://ваш-домен.com',
]
```

## 📊 Что Должно Работать

После применения исправлений:

✅ Вход на сайт с телефона  
✅ Решение задач  
✅ **Доступ к профилю** 🎯  
✅ **Доступ к рейтингу** 🎯  
✅ Просмотр статистики  
✅ Все остальные функции  

## 🆘 Если Проблема Остается

1. **Проверьте логи сервера** - там будет информация об ошибках
2. **Проверьте консоль браузера** (F12 на телефоне через Chrome DevTools)
3. **Попробуйте другой браузер** на телефоне
4. **Проверьте, что DEBUG=False** на продакшене

## 📝 Контрольный Список

- [x] `SESSION_COOKIE_SECURE = not DEBUG`
- [x] `SESSION_COOKIE_SAMESITE = 'Lax'`
- [x] `CSRF_COOKIE_SECURE = not DEBUG`
- [x] `CSRF_COOKIE_SAMESITE = 'Lax'`
- [x] Добавлено логирование
- [x] `ALLOWED_HOSTS` настроен правильно
- [ ] Изменения задеплоены на сервер
- [ ] Кэш браузера очищен
- [ ] Проверено на телефоне

---

**После применения всех изменений и деплоя, профиль и рейтинг должны работать на мобильных устройствах!** 🎉
