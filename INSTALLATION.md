# 🚀 Руководство по Установке Платформы "Ал Хоразми"

## Быстрый Старт (Windows)

### Вариант 1: Автоматическая установка

1. **Откройте командную строку** в папке проекта
2. **Запустите скрипт установки:**
   ```bash
   setup.bat
   ```
3. **Следуйте инструкциям** на экране
4. **Запустите сервер:**
   ```bash
   run.bat
   ```
5. **Откройте браузер:** http://localhost:8000

### Вариант 2: Ручная установка

#### Шаг 1: Создание виртуального окружения

```bash
python -m venv venv
venv\Scripts\activate
```

#### Шаг 2: Установка зависимостей

```bash
pip install -r requirements.txt
```

#### Шаг 3: Применение миграций

```bash
python manage.py migrate
```

#### Шаг 4: Создание суперпользователя

```bash
python manage.py createsuperuser
```

Введите:
- Username (имя пользователя)
- Email
- Password (пароль)

#### Шаг 5: Загрузка примеров задач

```bash
python manage.py load_sample_problems
```

#### Шаг 6: Запуск сервера

```bash
python manage.py runserver
```

#### Шаг 7: Открытие приложения

Откройте браузер и перейдите по адресу:
- **Главная страница:** http://localhost:8000
- **Админ-панель:** http://localhost:8000/admin

---

## Настройка для Linux/Mac

### Шаг 1: Создание виртуального окружения

```bash
python3 -m venv venv
source venv/bin/activate
```

### Шаг 2: Установка зависимостей

```bash
pip install -r requirements.txt
```

### Шаг 3-7: Аналогично Windows

Выполните команды из шагов 3-7 выше.

---

## Использование PostgreSQL (опционально)

По умолчанию проект использует SQLite. Для использования PostgreSQL:

### 1. Установите PostgreSQL

Скачайте и установите PostgreSQL с официального сайта.

### 2. Создайте базу данных

```sql
CREATE DATABASE al_khwarizmi_db;
CREATE USER al_khwarizmi_user WITH PASSWORD 'your_password';
GRANT ALL PRIVILEGES ON DATABASE al_khwarizmi_db TO al_khwarizmi_user;
```

### 3. Обновите файл .env

```env
USE_POSTGRESQL=True
DB_NAME=al_khwarizmi_db
DB_USER=al_khwarizmi_user
DB_PASSWORD=your_password
DB_HOST=localhost
DB_PORT=5432
```

### 4. Установите psycopg2

```bash
pip install psycopg2-binary
```

### 5. Примените миграции

```bash
python manage.py migrate
```

---

## Структура Проекта

```
algoritm/
├── al_khwarizmi/          # Главный проект Django
│   ├── settings.py        # Настройки
│   ├── urls.py            # URL маршруты
│   └── wsgi.py            # WSGI конфигурация
├── users/                 # Приложение пользователей
│   ├── models.py          # Модель Profile
│   ├── views.py           # API endpoints
│   └── serializers.py     # Сериализаторы
├── problems/              # Приложение задач
│   ├── models.py          # Topic, Problem, UserAttempt
│   ├── views.py           # API для задач
│   └── management/        # Команды загрузки данных
├── arena/                 # Приложение арены
│   ├── models.py          # ArenaRank
│   └── management/        # Команда обновления рейтингов
├── templates/             # HTML шаблоны
│   └── index.html         # Главная страница
├── static/                # Статические файлы
│   └── app.js             # Frontend JavaScript
├── manage.py              # Django management
├── requirements.txt       # Зависимости Python
└── README.md              # Документация
```

---

## API Endpoints

### Аутентификация
- `POST /api/auth/register/` - Регистрация
- `POST /api/auth/login/` - Вход
- `POST /api/auth/logout/` - Выход
- `POST /api/auth/token/refresh/` - Обновление токена

### Пользователи
- `GET /api/user/profile/` - Получить профиль
- `PUT /api/user/profile/` - Обновить профиль
- `GET /api/user/progress/` - Прогресс по темам

### Задачи
- `GET /api/problems/generate/` - Получить новую задачу
- `POST /api/problems/submit/` - Отправить решение
- `GET /api/problems/attempts/` - История попыток
- `GET /api/problems/topics/` - Список тем

### Арена
- `GET /api/arena/leaderboard/` - Таблица лидеров
- `GET /api/arena/stats/` - Статистика пользователя

---

## Управление через Django Admin

1. Перейдите на http://localhost:8000/admin
2. Войдите с учетными данными суперпользователя
3. Управляйте:
   - Пользователями и профилями
   - Темами и задачами
   - Попытками решений
   - Рейтингами арены

---

## Добавление Новых Задач

### Через Django Admin

1. Войдите в админ-панель
2. Перейдите в раздел "Задачи"
3. Нажмите "Добавить задачу"
4. Заполните поля:
   - Тема
   - Заголовок
   - Формула (LaTeX)
   - Описание
   - Правильный ответ
   - Сложность (0-3000)
   - Шаги решения (JSON)
   - Подсказки (JSON)

### Программно

Создайте management command или используйте Django shell:

```python
from problems.models import Topic, Problem

topic = Topic.objects.get(name='Алгебра: Квадратные уравнения')

Problem.objects.create(
    topic=topic,
    title='Новая задача',
    latex_formula=r'x^2 + 5x + 6 = 0',
    description='Решите уравнение',
    correct_answer='-2, -3',
    difficulty_score=900,
    solution_steps=['Шаг 1...', 'Шаг 2...'],
    hints=['Подсказка 1', 'Подсказка 2'],
    is_active=True
)
```

---

## Обновление Рейтингов Арены

Для обновления рейтингов арены выполните:

```bash
python manage.py update_arena_ranks
```

Рекомендуется настроить автоматический запуск этой команды раз в неделю через cron (Linux) или Task Scheduler (Windows).

---

## Решение Проблем

### Ошибка: "No module named 'decouple'"

```bash
pip install python-decouple
```

### Ошибка: "django.db.utils.OperationalError"

Убедитесь, что:
1. База данных создана
2. Миграции применены: `python manage.py migrate`
3. Настройки в .env корректны

### Ошибка: "Port 8000 is already in use"

Используйте другой порт:
```bash
python manage.py runserver 8080
```

### Проблемы с CORS

Убедитесь, что в .env указаны правильные CORS_ALLOWED_ORIGINS.

---

## Тестирование

Запуск тестов:

```bash
python manage.py test
```

Запуск с покрытием кода:

```bash
pip install coverage
coverage run --source='.' manage.py test
coverage report
```

---

## Деплой в Production

### Важные шаги:

1. **Измените SECRET_KEY** в .env
2. **Установите DEBUG=False**
3. **Настройте ALLOWED_HOSTS**
4. **Используйте PostgreSQL**
5. **Настройте HTTPS**
6. **Соберите статические файлы:**
   ```bash
   python manage.py collectstatic
   ```
7. **Используйте gunicorn или uWSGI**
8. **Настройте nginx как reverse proxy**

---

## Поддержка

Если возникли проблемы:
1. Проверьте логи Django
2. Убедитесь, что все зависимости установлены
3. Проверьте файл .env
4. Изучите документацию Django

---

## Лицензия

MIT License

---

**Удачи в использовании платформы Ал Хоразми! 📐**
